void setup (){
 size(500,500);
 background(0);
 fill(255);
 noStroke();
}

void draw () { 
int circle = 30;
while (circle<width-18){
  int circlex = 30; 
  while (circlex<height-18){ 
    ellipse(circle,circlex,15,15);
    circlex=circlex + 20;
  }
  circle=circle+20;
}
}
